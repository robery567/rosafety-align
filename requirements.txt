# Core ML stack — pin compatible with Paper 2 where possible
torch>=2.4
transformers>=4.51   # native Phi-4, Gemma-3 support; Paper 2 fix-up baseline
accelerate>=0.34
bitsandbytes>=0.44

# Alignment training
trl>=0.11             # DPO, KTO, ORPO trainers
peft>=0.13            # LoRA / probe-guided LoRA
deepspeed>=0.15       # only used when full-DPO baseline OOMs on A100-40G
datasets>=3.0

# Inference / eval
vllm>=0.6
lm-eval>=0.4.5        # standardised EN suite
sacrebleu>=2.4        # Flores RO-EN
sacremoses>=0.1

# LLM judge harness (reused from Paper 2)
openai>=1.40          # OpenRouter wrapper
tenacity>=9.0
diskcache>=5.6

# Translation control (no LLM-MT — Paper 2 R8 lesson)
deep-translator>=1.11

# Data + analysis
pyyaml>=6.0
pandas>=2.0
numpy>=1.24
tqdm>=4.66
jsonlines>=4.0
matplotlib>=3.8
seaborn>=0.13
scikit-learn>=1.4
scipy>=1.11           # Wilson CIs, Spearman exact-distribution

# Experiment tracking
wandb>=0.18

# Release
huggingface_hub>=0.25
zenodo-client>=0.3
